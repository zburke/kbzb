# Before `make install' is performed this script should be runnable with
# `make test'. After `make install' it should work as `perl kbzb.t'

#########################

# change 'tests => 1' to 'tests => last_test_to_print';

#use Test::More tests => 1;
#BEGIN { use_ok('kbzb::helpers::html', qw(heading ul ol _list br img doctype link_tag meta nbs) ) };

#########################

# Insert your test code below, the Test::More module is use()ed here so read
# its man page ( perldoc Test::More ) for help writing this test script.
#is(heading('foo'), 'expected', 'testing heading');
#is(ul('foo'), 'expected', 'testing ul');
#is(ol('foo'), 'expected', 'testing ol');
#is(_list('foo'), 'expected', 'testing _list');
#is(br('foo'), 'expected', 'testing br');
#is(img('foo'), 'expected', 'testing img');
#is(doctype('foo'), 'expected', 'testing doctype');
#is(link_tag('foo'), 'expected', 'testing link_tag');
#is(meta('foo'), 'expected', 'testing meta');
#is(nbs('foo'), 'expected', 'testing nbs');
